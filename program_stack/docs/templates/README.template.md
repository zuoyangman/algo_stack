# <Algorithm Name>

> One-sentence description.

## Run

From `program_stack/`:

```bash
./scripts/run.sh <category>/<algorithm_name> java
./scripts/run.sh <category>/<algorithm_name> cpp
./scripts/run.sh <category>/<algorithm_name> rust
./scripts/run.sh <category>/<algorithm_name> go
```

## Public API (aligned across languages)

| Operation | Description |
| --------- | ----------- |
| `sort(a)` / `search(a, x)` / … | … |

## Notes

- Each language demo self-checks a few fixed cases and prints `<name>: ok`.

## Where to go next

- Theory → [`PRINCIPLE.md`](PRINCIPLE.md)
- Extending → [`EXTENSION.md`](EXTENSION.md)
